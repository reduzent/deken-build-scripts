#!/bin/bash
pdsrcdir=~/pd-src
pddir=${pdsrcdir}/pd/
platform=$(basename $(pwd))
workspacedir=${pdsrcdir}/workspace/${platform}
remoteworkspacedir=pd-src/workspace/${platform}
remotehost=roman@netpd.org
datecommand="date -d @"
localdep=false
ocaldep_platform=linux
MAKEFLAGS="CC=arm-linux-gnueabihf-gcc-8 CFLAGS=\"-march=armv7\""
